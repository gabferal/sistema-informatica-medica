{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "python -c "import os, subprocess; port = os.environ.get('PORT', 8080); subprocess.call(['gunicorn', '--bind', f'0.0.0.0:{port}', 'app:app'])"",
    "preDeployCommand": "python database.py"
  }
}