{
    "$schema": "https://railway.app/railway.schema.json",
    "deploy": {
        "startCommand": "printenv && gunicorn --bind 0.0.0.0:$PORT app:app",
        "preDeployCommand": "python database.py"
    }
}